spring.application.name=ecommerce

# Configuración de la conexión a MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db?serverTimezone=UTC&useSSL=false
spring.datasource.username=root
spring.datasource.password=Minerva2025.

# Estrategia de JPA: "update" crea o actualiza las tablas automáticamente según tus clases Java
spring.jpa.hibernate.ddl-auto=update

# Muestra en la consola de VSC las consultas SQL que Spring hace por detrás (muy útil para estudiar)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true